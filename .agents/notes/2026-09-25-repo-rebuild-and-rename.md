# 决策：以 dsh-workbuddy-bridge 重做本仓（2026-09-25）

已实施。

## 问题

接手的是 `corrinehu/dsh-workbuddy-connect`（历史与命名归原作者）。维护者要求按自己的规范重做：新仓库名、新目录名、新包名，且不保留原作者的提交历史。

## 决策

- 仓库名、根目录名、包名统一为 `dsh-workbuddy-bridge`
- 旧历史整段删除**之前**先备份到仓外（bundle + `.git` 副本），经 `git bundle verify` 与备份内 `git log` 双重确认 5 个提交都在，才执行删除
- 新历史从 `0.1.0` 起，提交全部归本仓维护者
- 原作者版权与 MIT 许可**完整保留**，来源（原仓 URL + 原提交 `3dad637`）记入 [NOTICE](../../NOTICE)

## 替代方案

- 保留原历史再改名：历史里仍是原作者的提交，不满足要求
- fork：同样是原历史，且保留分叉关系
- 不备份直接删 `.git`：不可逆，失败无法回滚

## 影响

- 失去原仓的提交粒度（可从仓外备份回查）
- 换来源清晰、命名与文档网络统一

## 关联

[NOTICE](../../NOTICE) · [LICENSE](../../LICENSE)